# Museum — 个人收藏馆 产品需求文档 (PRD)

> 版本：v0.1 MVP
> 日期：2026-05-31
> 状态：规划中

---

## 一、产品概述

### 背景与痛点

手机相册里存了大量有纪念意义的物品照片——玩具、礼物、旅行纪念品、限定款收藏……但**拍完之后几乎再也不会打开**。这些记忆被"存"起来了，却没有被"看见"。

### 产品定位

**Museum** 是一个虚拟个人收藏馆。用户通过拍照，借助 AI 将现实物品数字化，摆入专属的 3D 虚拟空间，随时"逛"自己的收藏馆，重新建立与物品之间的情感连接。

### 核心价值

> 不是相册，是**有空间感的情感博物馆**。

| 维度 | 描述 |
|------|------|
| 目标用户 | 收藏爱好者、恋旧的人、手账族、二次元/潮玩收藏者 |
| 核心差异 | 空间展示 + 故事卡 + 记忆推送，三者缺一不可 |
| 平台 | iOS / Android App（MVP 先做移动端） |

---

## 二、MVP 范围

> **原则：先做情感机制，后做炫技效果。**
> MVP 哪怕用 2.5D 伪透视效果，只要推送机制和故事卡做好，用户就会留存。

### 包含功能（P0）

- [x] 拍照入库 + AI 自动抠图
- [x] 2.5D 虚拟房间展示（书架/展柜）
- [x] 物品故事卡（时间、地点、文字记录）
- [x] 「今日记忆」每日推送

### 不在 MVP 内（P1/P2，后续迭代）

- [ ] 真·3D 模型生成
- [ ] 房间风格切换
- [ ] AR 展示
- [ ] 社交分享 / 好友参观
- [ ] 分享海报生成

---

## 三、核心功能详细设计

### 3.1 拍照入库流程

```
用户拍照（或从相册选图）
    ↓
AI 自动抠图，去除背景，生成透明底图
    ↓
AI 识别物品类型，自动填写物品名称（用户可修改）
    ↓
用户选择摆放位置（哪个格子/哪层架子）
    ↓
填写故事卡（可选，但强引导）
    ↓
物品出现在 3D 房间对应位置 ✅
```

**技术方案：**
- 抠图：调用 [Remove.bg API](https://www.remove.bg/api) 或集成本地 RMBG 模型
- 物品识别：GPT-4o Vision 或 Gemini Vision API
- 图片存储：本地 + iCloud/对象存储备份

---

### 3.2 虚拟房间展示

MVP 阶段使用 **2.5D 视差方案**，而非完整 3D 引擎，降低开发复杂度。

**展示逻辑：**
- 一个固定视角的"房间"，包含若干展示格子（书架、展柜、桌面）
- 物品以抠图后的 PNG 展示在格子中，带轻微阴影和景深模糊制造立体感
- 支持手机陀螺仪微视差效果（轻微晃动，增强空间感）
- 点击物品 → 全屏放大 + 展示故事卡

**容量设计：**
- MVP 免费：1 个房间，最多 30 件物品
- 格子布局：3 列 × N 行，自动增加行数

**风格皮肤系统：**

| 风格 | 描述 | 氛围词 | 解锁方式 |
|------|------|--------|----------|
| 🪵 温暖木质书房 | 复古木质书架、暖黄台灯、地毯、旧书堆 | 温暖 / 治愈 / 怀旧 | ✅ 默认免费 |
| 🖤 赛博潮玩陈列室 | 黑色背景、霓虹灯边框、亚克力展柜、荧光打光 | 酷 / 炫 / 未来感 | 买断解锁 |
| 🏛️ 极简白色展馆 | 纯白墙面、金属展架、博物馆打光、干净留白 | 高级 / 克制 / 艺术感 | 买断解锁 |
| 🌿 自然系原木民宿 | 白墙木地板、绿植点缀、麻布质感、自然光 | 清新 / 日系 / ins 风 | 买断解锁 |
| 🚀 太空站收藏舱 | 金属舱壁、圆形舷窗、悬浮展示台、星空背景 | 神秘 / 未来 / 独特 | 订阅专属 |

**MVP 阶段只实现前 2 套皮肤**（木质书房 + 赛博潮玩），其余后续按用户反馈补充。

---

### 3.3 物品故事卡

每件入库物品拥有一张「档案卡」：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 物品名称 | 文字 | ✅ | AI 预填，用户可改 |
| 入库时间 | 日期 | ✅ | 自动记录 |
| 获得时间 | 日期 | ❌ | 用户填写 |
| 获得地点 | 文字/地图 | ❌ | 手动输入或地图选点 |
| 背后的故事 | 长文字 | ❌ | 核心引导项，placeholder 给情感提示语 |
| 标签 | 多选 | ❌ | 旅行纪念 / 礼物 / 童年 / 限定款 / 自购… |

**引导文案示例（placeholder）：**
> "这件东西是怎么来的？当时是什么心情？"

---

### 3.4 今日记忆推送

这是解决"拍完再也不看"问题的**核心机制**。

**推送开关（用户自主控制）：**

| 推送类型 | 默认状态 | 说明 |
|----------|----------|------|
| 今日记忆（每日随机一件藏品） | ✅ 开启 | 用户可随时关闭 |
| 周年纪念（入馆满 N 年） | ✅ 开启 | 用户可随时关闭 |
| 新增藏品提醒（长期未入库） | ❌ 关闭 | 用户可主动开启 |

**推送时间（用户自定义）：**
- 今日记忆：默认早 9 点，用户可在设置中任意调整时间
- 周年纪念：事件当天固定推送，不可调整时间（但可关闭）
- 若当天藏品数为 0，今日记忆推送自动跳过

**推送内容：**
- 随机选取一件收藏，展示物品图片 + 故事卡摘要
- 若该藏品没有故事，只展示图片 + 入库时间

**通知文案示例：**
> 🏛️ 今日藏品：**初音未来手办**
> "2019年生日，朋友送的，当时很感动"
> —— 2019年3月15日 · 北京

> 📦 **入馆周年纪念**：扭蛋机小熊 入馆满 2 年了，去看看它？

**设置页入口：**
```
设置 → 推送通知
├── 今日记忆        [开关]  推送时间：09:00 [可点击修改]
├── 周年纪念        [开关]
└── 长期未入库提醒  [开关]  超过 N 天未添加藏品时提醒
```

---

## 四、用户旅程

```
第一次使用
├── 进入 App → 看到空的 3D 房间（空房间有引导动效）
├── 点击「+ 添加第一件藏品」
├── 拍照 → AI 抠图（loading 动效要精致）
├── AI 填好名称 → 用户选格子位置
├── 弹出故事卡（柔和引导，可跳过）
└── 物品「落」到格子里，动效 ✨

日常使用
├── 收到「今日记忆」推送 → 点击打开
├── 看到那件藏品 + 当时写的故事
├── 情感触发 → 可能补充故事 / 添加新藏品
└── 逛一逛房间，回忆涌现
```

---

## 五、界面结构（页面清单）

```
App
├── 首页（房间视图）          ← 核心页面，2.5D 房间
├── 物品详情页                ← 放大展示 + 故事卡
├── 添加物品流程（多步骤）
│   ├── Step1: 拍照/选图
│   ├── Step2: 确认抠图效果
│   ├── Step3: 选择位置
│   └── Step4: 填写故事卡
├── 今日记忆页                ← 推送跳转落地页
└── 设置页
    ├── 推送时间设置
    ├── 账号/备份
    └── 关于
```

---

## 六、架构决策：本地买断 vs 有后端

### 核心矛盾分析

产品有两个 AI 能力依赖持续产生费用：
- **抠图**：Remove.bg 按次计费 / 本地模型需要大包体
- **物品识别**：GPT-4o Vision 无法本地运行

这决定了纯买断制难以覆盖持续运营成本。

### 方案对比

| | 纯本地买断 | 有后端（混合模式）|
|---|---|---|
| AI 抠图 | 本地 RMBG 模型，包体 +200MB | 后端代理 API，体验更好 |
| 物品识别 | ❌ 无法实现 | ✅ 后端调 GPT-4o |
| 今日推送 | ✅ 本地通知，完全可行 | ✅ 可做服务端推送 |
| 数据备份 | ❌ 换手机数据丢失 | ✅ 云同步，用户强需求 |
| 收费模式 | 一次买断（¥30-98） | 买断 + 云服务订阅 |
| 运营成本 | 极低 | 服务器 + API 持续产生 |
| 用户隐私感 | 高（无账号） | 需登录，有顾虑 |

### ✅ 决策：混合方案（本地优先 + 可选后端增强）

```
本地核心（买断，永久可用，无需网络）
├── 本地 SQLite 存储所有数据
├── 本地 RMBG 模型抠图（离线可用）
├── 本地推送通知
└── 完整房间展示体验

后端增强（可选，订阅解锁）
├── GPT-4o 智能识别物品名称
├── 云端备份 & 多设备同步
└── 更高质量 AI 抠图（Remove.bg）
```

**商业模式：**
- 买断 ¥45，包含所有本地功能，永久可用
- 「云服务」¥12/月 或 ¥88/年，解锁云同步 + 智能识别

### MVP 阶段实际做法

> MVP 先不做后端，专注验证核心体验。

- 抠图：前端直接调 Remove.bg API（用自己的 key，MVP 阶段成本自担）
- 物品识别：**暂时跳过，用户手动输入名称**（后端上线后再补）
- 无登录、无云同步、纯本地存储

---

## 七、技术选型

### 已确认技术栈

| 模块 | 方案 | 说明 |
|------|------|------|
| **移动端框架** | **React Native (Expo)** | ✅ 已确认 |
| 2.5D 渲染 | React Native Reanimated + 视差动效 | 无需 3D 引擎，性能好 |
| AI 抠图（MVP） | Remove.bg API（前端直调） | 简单稳定，MVP 首选 |
| AI 抠图（后期） | 后端代理 + 本地 RMBG 模型 | 降低成本，支持离线 |
| 物品识别（后期） | 后端调 GPT-4o Vision | MVP 阶段跳过 |
| 本地存储 | Expo SQLite | 物品元数据 |
| 图片存储 | 本地文件系统 | MVP 阶段不做云同步 |
| 推送通知 | Expo Notifications | 跨平台方案 |

### 后端技术栈（MVP 后引入）

| 模块 | 方案 | 说明 |
|------|------|------|
| **后端语言** | **Go（Gin / Fiber）** | ✅ 已确认，性能好、部署简单 |
| 数据库 | PostgreSQL | 用户数据存储 |
| 图片存储 | 对象存储（阿里云 OSS / S3） | 云端图片备份 |
| 部署 | 单台 VPS 起步 | 阿里云/腾讯云 2核4G，约 ¥100/月 |

Go 做这类 API 服务非常适合——并发强、内存占用低、单二进制部署方便。

---

## 八、开发阶段规划

### Phase 1 — 核心闭环（预计 3-4 周）

目标：跑通「拍照 → 入库 → 房间展示 → 点击查看」完整流程

- [ ] 项目初始化，技术栈搭建
- [ ] 拍照 + 相册选图
- [ ] Remove.bg 抠图集成
- [ ] 物品数据结构设计 & 本地存储
- [ ] 2.5D 房间基础渲染（固定布局）
- [ ] 物品详情页（故事卡展示）

### Phase 2 — 情感机制（预计 2 周）

目标：让用户有理由每天打开

- [ ] 故事卡编辑（完整字段）
- [ ] 今日记忆推送（定时 + 随机选取）
- [ ] 周年纪念推送
- [ ] 物品放置动效优化

### Phase 3 — 打磨体验（预计 2 周）

目标：体验达到可以给朋友展示的程度

- [ ] GPT-4o 物品自动识别
- [ ] 陀螺仪视差效果
- [ ] 空房间引导动效
- [ ] 整体 UI 视觉打磨
- [ ] iOS & Android 测试

### Phase 4 — 后端上线（云服务，预计 3-4 周）

目标：推出云服务订阅，覆盖运营成本

- [ ] Go 后端项目初始化（Gin/Fiber）
- [ ] 用户账号体系（手机号 / Apple 登录）
- [ ] 后端代理 Remove.bg + GPT-4o 识别
- [ ] 图片云端备份（OSS）
- [ ] 多设备数据同步
- [ ] 订阅支付接入（App Store / Google Play）

---

## 九、成功指标（MVP 验证目标）

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 入库转化率 | > 80% | 打开 App 后完成第一件入库的比例 |
| 故事卡填写率 | > 50% | 入库时填写了故事的比例 |
| 次日留存 | > 40% | 隔天再次打开 App |
| 7日留存 | > 20% | 推送机制是否有效 |
| 人均入库数 | > 5件 | 用户是否真的在用 |

---

## 十、待决策问题

> 技术栈已确认，剩余待拍板项：

1. ~~**技术栈**：用 React Native 还是 Flutter？~~ ✅ **React Native (Expo)**
2. ~~**后端语言**~~ ✅ **Go（Gin/Fiber）**，MVP 后引入
3. ~~**账号体系**：MVP 要不要做登录？~~ ✅ **MVP 不做登录，纯本地**
4. **AI 成本**：Remove.bg MVP 阶段用自己的 key，单次 $0.02，预计测试阶段总花费 < $10，可接受
5. ~~**房间风格**：MVP 先做一套固定风格，哪种调性？~~ ✅ **皮肤系统，5套风格，见 3.2 节**

---

*下一步：所有决策已拍板，可以开始 Phase 1 开发（React Native + Expo 项目初始化）。*
